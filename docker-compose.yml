version: "3.9"

services:
  app:
    build: .
    restart: unless-stopped

    environment:
      - TZ=Asia/Seoul
      - SCREEN_YMD: ${SCREEN_YMD}

    env_file:
      - .env

    volumes:
      - ./logs/${SCREEN_YMD}:/app/logs