version: "3.9"

services:
  app:
    build: .
    container_name: cgv-watch
    restart: on-failure

    env_file:
      - .env

    environment:
      - TZ=Asia/Seoul

    volumes:
      - ./logs:/app/logs