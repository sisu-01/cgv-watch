version: "3.9"

services:
  app:
    image: cgv-watch-app
    build: .
    restart: unless-stopped

    environment:
      TZ: Asia/Seoul
      SCREEN_YMD: ${SCREEN_YMD}
      IS_DEV: ${IS_DEV:-false}   # 기본값 false

    env_file:
      - .env

    volumes:
      - ./logs/${SCREEN_YMD}:/app/logs